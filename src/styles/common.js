import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
  flex1: {
    flex: 1
  },
  flex1Centered: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  }
});
